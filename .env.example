# CLI별 인증 방법:
#
# Claude  — `claude` CLI 로그인 (별도 키 불필요)
# Codex   — `codex auth` 또는 OPENAI_API_KEY 설정
# Gemini  — `gemini auth` 또는 GEMINI_API_KEY 설정
#
# 아래는 Codex/Gemini를 API 키로 인증할 때만 필요
OPENAI_API_KEY=
GEMINI_API_KEY=
